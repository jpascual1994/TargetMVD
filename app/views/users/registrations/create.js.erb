$('#error-explanation').remove();
<% if resource.errors.empty? %>
  $('.left-container').replaceWith("<%= j render partial: 'users/registrations/confirmation_sent.html.haml', layout: false %>");
<% else %>
  $('#new_user').prepend("<%= devise_error_messages! %>");
<% end %>

