$('.home-left-top').addClass('hidden');
$('.home-left-chat-section').remove();
$('.home-left').prepend("<%= j render partial: 'chat.html.haml', layout: false,\
  locals: { chat: chat, target: chat.match.other_user_target(current_user) } %>")
